package com.colegiocursosms.domain.enums;

public enum ContentType {
      FOLDER,    // Carpeta contenedora
      FILE,      // Archivo (PDF, DOCX, etc.)
      LINK,      // Enlace externo (URL)
      ASSIGNMENT,// Tarea que espera una entrega
      EXAM,      // Examen
      FORUM      // Foro de discusión
}